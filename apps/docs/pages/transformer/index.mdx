# Data transformations

Our proxy automatically detects the data type that is being accessed from a S3 bucket and applies the appropriate transformations to the data.
The transformations can be defined in rego policy files and are inherently associated with purposes.
The next sections will explain the transformations we currently support.
